// Función para crear un contador de visitas
export function crearContadorVisitas() {
  return function incrementar() {
    // Función vacía - el contador se maneja en main.js con localStorage
  };
}
