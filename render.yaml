services:
  - type: web
    name: empaticos-backend
    env: node
    buildCommand: cd backend && npm install
    startCommand: node backend/src/app.js
    envVars:
      - key: PORT
        value: 5000
      - key: MONGO_URI
        sync: false
